// Firebase flow disabled.
// Keeping function exports to avoid breaking imports while removing runtime Firebase initialization.

export const initFirebase = () => null;

export const initAnalytics = async () => null;

export const initMessaging = async () => null;

export const requestNotificationPermission = async () => null;

export const setupForegroundMessaging = () => {};

export const app = null;
export const analytics = null;
export const messaging = null;
